name: flutter_app
description: "Fortune Fusion - AI 종합 운세 슈퍼앱"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # 07단계 Flutter 앱 설계서 §1.3 확정 패키지 (버전 고정)
  provider: 6.1.5+1         # 상태관리
  http: 1.5.0                # API 통신 (10단계 초기: Mock/Real 전환 가능한 ApiClient)
  shared_preferences: 2.5.3  # 토큰/온보딩완료/테마 등 경량 키값 저장
  hive: 2.2.3                 # 구조화 로컬 캐시(히스토리/오프라인)
  hive_flutter: 1.1.0         # Hive Flutter 통합
  intl: ^0.19.0               # 날짜/숫자 포맷팅(사주/운세/포인트 표시)

  # 07단계(추가, 수정) - AI 손금/관상 분석 이미지 입력 기능(§3.3)
  # 07단계(추가, 수정2) - camera 패키지(getUserMedia 기반 실시간 미리보기)는 모바일
  # 브라우저 환경에서 초기화 실패가 잦아 제거하고, image_picker의 ImageSource.camera
  # (OS 네이티브 카메라 앱 호출 - 웹에서는 <input capture> 방식으로 폴백)로 통일한다.
  image_picker: 1.2.2          # 갤러리 선택 + 카메라 촬영(OS 네이티브 카메라 호출)

  # CMS 제휴광고(배너) 클릭 시 외부 제휴 링크(쿠팡파트너스 등)를 열기 위한 패키지
  url_launcher: 6.3.1

  # CMS 제휴광고 "광고소스형"(제휴사가 발급한 원본 <iframe>/<script> 태그) 렌더링:
  # - Android/iOS: webview_flutter로 loadHtmlString() 렌더링
  # - Web: package:web + dart:ui_web으로 원본 iframe을 DOM에 직접 삽입(HtmlElementView)
  webview_flutter: 4.9.0
  web: ^1.1.1

  # "오늘의 행운숫자" 관리자 콘텐츠(영상) 재생용. [사용자 요청] 이 기능은 광고(ad_banner)와
  # 무관한 별도 콘텐츠이므로 webview_flutter가 아닌 video_player로 네이티브 영상 재생을 구현한다.
  video_player: 2.9.2

  # [오늘의 운세 표준 플로우] §6 후속연결 "공유 → 이미지 생성 후 공유" 구현용.
  # RepaintBoundary로 캡처한 결과 카드를 임시 파일로 저장(path_provider)한 뒤
  # 네이티브 공유 시트로 전달(share_plus). 둘 다 Web 플랫폼도 지원한다.
  share_plus: 10.1.4
  path_provider: 2.1.5

  # [타로 섹션 전면 개편 §11 P5 사운드 시스템] 카드 셔플/탭/리빌 임팩트/
  # 저장완료/UI탭 등 짧은 SFX 재생용. Web/Android 모두 지원.
  audioplayers: ^6.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/wish_counsel/characters/
    - assets/audio/tarot/
    - assets/tarot/cards/majors/
    - assets/tarot/cards/cups/
    - assets/tarot/cards/wands/
    - assets/tarot/cards/swords/
    - assets/tarot/cards/pentacles/
    - assets/tarot/thumbs/
    - assets/tarot/backs/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # [운세 앱 개발 프롬프트-메인 UI 리뉴얼] 현대카드 앱 스타일 참고 - Pretendard 폰트 적용
  # (Pretendard: 우아한형제들/orioncactus 배포, OFL 1.1 라이선스, 상업적 이용 무료)
  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard-Regular.ttf
          weight: 400
        - asset: assets/fonts/Pretendard-Medium.ttf
          weight: 500
        - asset: assets/fonts/Pretendard-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Pretendard-Bold.ttf
          weight: 700
        - asset: assets/fonts/Pretendard-ExtraBold.ttf
          weight: 800
    # [상담(Midnight Comfort) 전용 폰트 3종] wish_counsel 모듈 내부에서만
    # 참조하며(WishCounselText), 앱 전역 Pretendard 체계와는 별도로 둔다.
    - family: NotoSerifKRWish
      fonts:
        - asset: assets/fonts/wish_room/NotoSerifKR-Bold.ttf
          weight: 700
        - asset: assets/fonts/wish_room/NotoSerifKR-Black.ttf
          weight: 900
    - family: GowunBatangWish
      fonts:
        - asset: assets/fonts/wish_room/GowunBatang-Regular.ttf
          weight: 400
        - asset: assets/fonts/wish_room/GowunBatang-Bold.ttf
          weight: 700
    - family: IBMPlexMonoWish
      fonts:
        - asset: assets/fonts/wish_room/IBMPlexMono-Regular.ttf
          weight: 400
        - asset: assets/fonts/wish_room/IBMPlexMono-Medium.ttf
          weight: 500
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
